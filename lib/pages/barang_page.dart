import 'package:flutter/material.dart';

class BarangPage extends StatelessWidget {
  const BarangPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("barang"));
  }
}
